# Back-end

Base do backend com Express e MySQL.

## Scripts

- `npm run dev` - inicia com nodemon
- `npm start` - inicia em produção

## Variáveis de ambiente

Copie `.env.example` para `.env` e ajuste os valores do MySQL.
